# prisma

Placeholder for the Prisma schema and migrations.

The PostgreSQL + Prisma data foundation is introduced in **Phase 1 — Identity,
organizations, and tenant isolation** (see `docs/Roadmap.md`). Phase 0 does not
require a database.
