/**
 * @app/database — PostgreSQL + Prisma schema and organization-scoped repositories. Implemented in Phase 1.
 *
 * Phase 0 placeholder: this package establishes the module boundary described
 * in CLAUDE.md and SystemArchitecture.md. It intentionally contains no
 * business logic yet.
 */
export const PACKAGE_NAME = "@app/database";
