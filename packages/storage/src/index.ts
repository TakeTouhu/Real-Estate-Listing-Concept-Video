/**
 * @app/storage — Object-storage abstraction: short-lived signed URLs and managed-storage copy of provider outputs. Implemented in Phase 2 and Phase 4.
 *
 * Phase 0 placeholder: this package establishes the module boundary described
 * in CLAUDE.md and SystemArchitecture.md. It intentionally contains no
 * business logic yet.
 */
export const PACKAGE_NAME = "@app/storage";
