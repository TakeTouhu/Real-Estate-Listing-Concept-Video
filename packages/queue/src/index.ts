/**
 * @app/queue — Job queue and worker plumbing (enqueue, heartbeat, retry, dead-letter). Implemented in Phase 4.
 *
 * Phase 0 placeholder: this package establishes the module boundary described
 * in CLAUDE.md and SystemArchitecture.md. It intentionally contains no
 * business logic yet.
 */
export const PACKAGE_NAME = "@app/queue";
