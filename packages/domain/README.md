# @app/domain

Provider-agnostic domain models, invariants, and services (identity, property, storyboard, generation, billing rules). Implemented from Phase 1 onward.

Phase 0 placeholder — module boundary only, no business logic yet.
